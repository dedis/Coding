#!/bin/bash

B_NBR=$1

if [ "$B_NBR" ]; then
  BRANCH=$( git_nb $B_NBR )
  if [ "$BRANCH" ]; then
    git checkout $BRANCH
  else
    echo "Didn't find branch number $B_NBR"
  fi
else
  git_nb
fi

git stash list
